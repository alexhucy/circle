machine:
  node:
    version: 4.4.4
dependencies:
  pre:
    - npm install
test:
  override:
    - mocha test --reporter mocha-junit-reporter:
        environment:
          MOCHA_FILE: $CIRCLE_TEST_REPORTS/junit/test-results.xml

